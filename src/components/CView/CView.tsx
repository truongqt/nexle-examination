import {createBox, createRestyleComponent, VariantProps} from '@shopify/restyle';
import React from 'react';
import { Theme } from 'utils/themes/theme';

const CView = createBox<Theme>();

export default CView;